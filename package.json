{
  "name": "sam-ui-workspace",
  "private": true,
  "version": "0.1.0",
  "license": "MIT",
  "packageManager": "pnpm@9.0.0",
  "scripts": {
    "build": "pnpm -r --filter ./packages/* run build",
    "dev": "pnpm --filter @sam/ui dev & pnpm --filter playground dev",
    "test": "pnpm -r --filter ./packages/* test",
    "lint": "pnpm -r --filter ./packages/* lint"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ]
}